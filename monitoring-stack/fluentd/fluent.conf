<source>
  @type tcp
  port 24224
  bind 0.0.0.0
  format json
  tag springboot
</source>

<match springboot>
  @type elasticsearch
  host localhost
  port 9200
  index_name springboot
  logstash_format true
</match>
